# Database Configuration
POSTGRES_USER=cti_user
POSTGRES_PASSWORD=cti_password_dev
POSTGRES_DB=cti_ecommerce
DB_PORT=5432

# Backend Configuration
NODE_ENV=development
BACKEND_PORT=3000

# Frontend Configuration
FRONTEND_PORT=5173

# Database URL (used by Prisma in backend)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${DB_PORT}/${POSTGRES_DB}?schema=public
